# Anistroph environment configuration
# Copy this file to .env and adjust as needed.
# All values are optional — the application has sensible defaults.

# --- Dataset partitioning defaults ---
# Fractions of the full dataset, must sum to 1.0.
# Per-dataset YAML split: sections override these values.
TRAIN_DATASET_PCT=0.80
EVAL_DATASET_PCT=0.20
# Reserved for future validation-during-training partition.
VALIDATE_DATASET_PCT=0.0

# --- ngrok (optional, for ChatGPT GPT Actions via make start-gpt) ---
# Get a free token at https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_AUTHTOKEN=your-ngrok-authtoken-here

# --- Aina-Veris A2A integration (optional, for external tool registry) ---
# Base URL of the Aina-Veris A2A agent server. Used by the external tool
# registry to resolve the call_veris_semiconductor_research_agent endpoint.
# For local development (Aina-Veris running on host): http://localhost:8100
# From inside a Docker container: http://host.docker.internal:8100
# For Docker Compose: http://aina-veris:8100
# AINA_VERIS_BASE_URL=http://localhost:8100
